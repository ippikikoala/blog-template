# ブログ不具合検知レポート

デプロイされたブログ（https://www.ippikikoala.com/）の検査を実施し、複数の不具合を検出しました。

**検査日時**: 2026-01-09  
**検査対象**: https://www.ippikikoala.com/

---

## 📋 検出された不具合の概要

| # | 不具合 | 重要度 | ステータス |
|---|--------|--------|-----------|
| 1 | 「未分類」カテゴリで404エラー | 🔴 高 | 未修正 |
| 2 | カテゴリ一覧ページに「未分類」が表示されない | 🟡 中 | 未修正 |
| 3 | モバイルメニューが自動で閉じない | 🟡 中 | 未修正 |
| 4 | 画像プリロードの警告 | 🟢 低 | 未修正 |

---

## 🔴 Issue #1: 「未分類」カテゴリで404エラー

### 問題の詳細

サイドバーに表示されている「未分類 (12)」のリンクをクリックすると、**404 Page Not Found** エラーが発生します。

![404エラー画面](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/category_page_unclassified_1767964192281.png)

### 根本原因

1. **データの問題**: 11件の記事が `category: ""` （空文字列）を持っている
   - 例: `content/posts/2023-08-11-2023-08-11-011235.mdx`
   
2. **コードの問題**: [`src/lib/posts.ts`](file:///Users/ippiki_koala/Desktop/Claude/blog/src/lib/posts.ts#L97-L112) の `getAllCategories()` 関数が空文字列をフィルタリングしている

```typescript
export function getAllCategories(): { name: string; count: number }[] {
  const posts = getAllPosts();
  const categories = posts.reduce(
    (acc, post) => {
      if (post.category) {  // ← 空文字列は falsy なのでスキップされる
        acc[post.category] = (acc[post.category] || 0) + 1;
      }
      return acc;
    },
    {} as Record<string, number>
  );
  // ...
}
```

3. **矛盾**: サイドバーには「未分類 (12)」と表示されているが、実際には `getAllCategories()` が空文字列カテゴリを返していないため、静的生成時にそのページが作成されていない

### 影響範囲

- サイドバーのカテゴリリンク（全ページ）
- モバイルメニューのカテゴリリンク
- 11件の記事が実質的にカテゴリページからアクセス不可

### 推奨される修正方法

**オプション A**: 空文字列カテゴリを「未分類」として扱う

[`src/lib/posts.ts`](file:///Users/ippiki_koala/Desktop/Claude/blog/src/lib/posts.ts) を修正:

```typescript
export function getAllCategories(): { name: string; count: number }[] {
  const posts = getAllPosts();
  const categories = posts.reduce(
    (acc, post) => {
      const category = post.category || "未分類";  // 空文字列を「未分類」に変換
      acc[category] = (acc[category] || 0) + 1;
      return acc;
    },
    {} as Record<string, number>
  );
  // ...
}
```

**オプション B**: 空文字列カテゴリを完全に除外し、サイドバーにも表示しない

---

## 🟡 Issue #2: カテゴリ一覧ページに「未分類」が表示されない

### 問題の詳細

[カテゴリ一覧ページ](https://www.ippikikoala.com/categories)に「未分類」カテゴリが表示されていません。

![カテゴリ一覧ページ](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/categories_index_page_1767964218781.png)

### 根本原因

Issue #1と同じ根本原因です。`getAllCategories()` が空文字列カテゴリを返さないため、カテゴリ一覧ページにも表示されません。

### 影響範囲

- `/categories` ページ
- ユーザーが「未分類」カテゴリの記事を発見できない

### 推奨される修正方法

Issue #1の修正により自動的に解決されます。

---

## 🟡 Issue #3: モバイルメニューが自動で閉じない

### 問題の詳細

ブラウザをモバイルサイズ（375px幅）にリサイズしてハンバーガーメニューを開いた後、ブラウザをデスクトップサイズに戻すと、**モバイルメニューのオーバーレイが画面に残り続けます**。

![モバイルメニューが開いた状態](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/mobile_menu_open_1767964311015.png)

### 根本原因

[`src/components/MobileMenu.tsx`](file:///Users/ippiki_koala/Desktop/Claude/blog/src/components/MobileMenu.tsx) がウィンドウリサイズイベントを監視していないため、画面サイズが変わってもメニューの状態が自動的にリセットされません。

### 影響範囲

- モバイル↔デスクトップ間でブラウザをリサイズするユーザー
- レスポンシブデザインのテスト時

### 推奨される修正方法

`MobileMenu.tsx` に `useEffect` を追加してウィンドウリサイズを監視:

```typescript
useEffect(() => {
  const handleResize = () => {
    // デスクトップサイズ（768px以上）になったらメニューを閉じる
    if (window.innerWidth >= 768) {
      setIsMenuOpen(false);
    }
  };

  window.addEventListener('resize', handleResize);
  return () => window.removeEventListener('resize', handleResize);
}, []);
```

---

## 🟢 Issue #4: 画像プリロードの警告

### 問題の詳細

ブラウザコンソールに複数の警告が表示されています:

```
The resource was preloaded using link preload but not used within a few seconds...
```

### 根本原因

Next.jsの画像最適化機能が、実際には使用されない画像をプリロードしている可能性があります。これは通常、以下の原因で発生します:

- 条件付きレンダリングで画像が表示されない
- 画像のパスが動的に変更される
- プリロードの優先度設定が不適切

### 影響範囲

- パフォーマンスへの軽微な影響（不要なネットワークリクエスト）
- ユーザー体験への直接的な影響はなし

### 推奨される修正方法

1. Next.jsの画像コンポーネントの `priority` プロップを見直す
2. 実際に使用されていない画像のプリロードを削除
3. 動的画像の場合は遅延読み込みを使用

---

## ✅ 正常に動作している機能

以下の機能は正常に動作していることを確認しました:

- ✅ ホームページのレイアウトと記事一覧表示
- ✅ 個別記事ページの表示
- ✅ 目次（Table of Contents）の動作
- ✅ トップナビゲーション（Home, About, Categories, Tags）
- ✅ タグ一覧ページ
- ✅ プロフィールサイドバー
- ✅ モバイルメニューの開閉機能（リサイズ時を除く）
- ✅ レスポンシブデザイン

---

## 🎬 検査の様子

検査中のブラウザ操作は以下の動画に記録されています:

![検査の様子](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/blog_inspection_1767964115978.webp)

---

## 📸 スクリーンショット

````carousel
![ホームページ](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/homepage_overview_1767964144466.png)
<!-- slide -->
![記事ページ（Hello World）](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/post_page_hello_world_1767964171420.png)
<!-- slide -->
![404エラー（未分類カテゴリ）](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/category_page_unclassified_1767964192281.png)
<!-- slide -->
![カテゴリ一覧ページ](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/categories_index_page_1767964218781.png)
<!-- slide -->
![Aboutページ](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/about_page_1767964235837.png)
<!-- slide -->
![モバイル表示（About）](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/mobile_view_about_1767964251465.png)
<!-- slide -->
![モバイルメニュー展開](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/mobile_menu_open_1767964311015.png)
<!-- slide -->
![タグ一覧ページ](file:///Users/ippiki_koala/.gemini/antigravity/brain/c578c083-80ad-4c13-affb-5279b5b5bf36/tags_page_1767964467182.png)
````

---

## 🔧 次のステップ

1. **優先度 高**: Issue #1（未分類カテゴリの404エラー）を修正
2. **優先度 中**: Issue #3（モバイルメニューの自動クローズ）を修正
3. **優先度 低**: Issue #4（画像プリロード警告）を調査・修正

修正後は以下を実施してください:

- ローカル環境でビルド確認（`npm run build`）
- 各修正箇所の動作確認
- Vercelへのデプロイ
- 本番環境での最終確認
